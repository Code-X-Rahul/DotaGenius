---
phase: 01-replay-pipeline
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
files_modified:
  - src/workers/replay-worker.ts
  - src/app/match/[matchId]/page.tsx
  - src/app/page.tsx
  - tests/integration/replay-pipeline.test.ts
  - tests/integration/data-storage.test.ts
  - tests/integration/data-query.test.ts
  - scripts/start-worker.sh
  - package.json
autonomous: false
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03

must_haves:
  truths:
    - "User submits a Match ID on the landing page and is redirected to a status page"
    - "Status page shows live progress: downloading -> parsing -> complete"
    - "After completion, parsed data is queryable in PostgreSQL"
    - "Already-analyzed matches show cached results immediately"
    - "System rejects/warns about expired Match IDs (>10 days)"
    - "Async job queue processes replays without blocking the web server"
  artifacts:
    - path: "tests/integration/replay-pipeline.test.ts"
      provides: "End-to-end pipeline integration test"
      min_lines: 40
    - path: "tests/integration/data-storage.test.ts"
      provides: "Verifies parsed data stored correctly in PostgreSQL"
      min_lines: 30
    - path: "tests/integration/data-query.test.ts"
      provides: "Verifies match data queryable by match_id"
      min_lines: 20
    - path: "scripts/start-worker.sh"
      provides: "Script to start the BullMQ worker process"
  key_links:
    - from: "src/app/page.tsx (MatchIdInput)"
      to: "/api/matches/[matchId]"
      via: "POST fetch on form submit"
      pattern: "fetch.*api/matches"
    - from: "/api/matches/[matchId] POST"
      to: "src/lib/queue.ts"
      via: "Enqueues job"
      pattern: "replayQueue\\.add"
    - from: "src/workers/replay-worker.ts"
      to: "prisma"
      via: "Stores parsed data"
      pattern: "prisma\\."
    - from: "src/app/match/[matchId]/page.tsx"
      to: "/api/jobs/[jobId]/status"
      via: "SSE subscription"
      pattern: "EventSource"
---

<objective>
Wire the frontend and backend together into a working end-to-end pipeline, write integration tests, create worker startup scripts, and verify the complete flow works.

Purpose: Plans 03 and 04 built the backend and frontend independently. This plan connects them, ensures data flows correctly end-to-end, and adds integration tests that prove PIPE-01, PIPE-02, and PIPE-03 are satisfied.
Output: Fully integrated replay pipeline with integration tests and operational scripts.
</objective>

<execution_context>
@/home/rahulrajput/.claude/get-shit-done/workflows/execute-plan.md
@/home/rahulrajput/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-replay-pipeline/01-RESEARCH.md
@.planning/phases/01-replay-pipeline/01-01-SUMMARY.md
@.planning/phases/01-replay-pipeline/01-03-SUMMARY.md
@.planning/phases/01-replay-pipeline/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration wiring, worker scripts, and integration tests</name>
  <files>scripts/start-worker.sh, package.json, tests/integration/replay-pipeline.test.ts, tests/integration/data-storage.test.ts, tests/integration/data-query.test.ts, src/workers/replay-worker.ts, src/app/match/[matchId]/page.tsx, src/app/page.tsx</files>
  <action>
**Worker startup script** (`scripts/start-worker.sh`):
```bash
#!/bin/bash
npx tsx src/workers/replay-worker.ts
```
Make executable: `chmod +x scripts/start-worker.sh`

**Package.json scripts**: Add to scripts:
- `"worker": "tsx src/workers/replay-worker.ts"` -- start the BullMQ worker
- `"dev:all": "concurrently \"npm run dev\" \"npm run worker\""` -- start both Next.js and worker (install concurrently as dev dep if needed: `npm install -D concurrently`)
- `"db:push": "prisma db push"` -- push schema to database
- `"db:studio": "prisma studio"` -- open Prisma Studio
- `"docker:up": "docker compose up -d"` -- start dev services
- `"docker:down": "docker compose down"` -- stop dev services
- `"test": "vitest run"` -- run all tests
- `"test:unit": "vitest run tests/unit"` -- run unit tests only
- `"test:integration": "vitest run tests/integration"` -- run integration tests only

**Integration wiring fixes** (review and fix any issues):
- Ensure MatchIdInput POST call matches the API route structure
- Ensure status page reads jobId from the correct source (URL search params from the redirect)
- Ensure worker correctly references all imports from lib modules
- Ensure SSE endpoint correctly resolves the job from BullMQ
- Run `npx prisma db push` in the verify step to ensure schema applies

**Integration tests** (these require running services -- document that docker-compose must be up):

`tests/integration/data-storage.test.ts`:
- Test that a Match record can be created and all related records (Player, CombatLogEvent, PositionSnapshot) stored in a transaction
- Use actual Prisma client against test database
- Create match with matchId, duration, startTime, etc.
- Create 10 Player records for the match
- Create sample CombatLogEvent records
- Create sample PositionSnapshot records
- Verify all records exist after transaction
- Clean up test data after each test
- Skip tests if DATABASE_URL is not set (CI-friendly)

`tests/integration/data-query.test.ts`:
- Test querying match by matchId returns match with players
- Test querying combat_log_events by matchId and gameTime range
- Test querying position_snapshots by matchId and heroId
- Verify indexes make queries efficient (no full table scans)
- Skip tests if DATABASE_URL is not set

`tests/integration/replay-pipeline.test.ts`:
- Test the POST /api/matches/[matchId] endpoint returns 202 with jobId (mock the queue add)
- Test the GET /api/matches/[matchId] endpoint returns 404 for unknown match
- Test the GET /api/matches/[matchId] endpoint returns match data for existing complete match
- Test duplicate submission returns existing job (same matchId as job ID)
- These use Next.js test utilities or direct function imports

Note: Integration tests that require external services (Redis, PostgreSQL) should be guarded with `describe.skipIf(!process.env.DATABASE_URL)` so they don't fail in environments without services.
  </action>
  <verify>
    <automated>cd /home/rahulrajput/Documents/personal-projects/DotaGenius && npx vitest run tests/unit/ --reporter=verbose 2>&1 | tail -10 && echo "---unit done---" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Worker startup script exists, package.json has all operational scripts, integration test files exist with proper skip guards, all unit tests still pass, TypeScript compiles without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete replay pipeline end-to-end</name>
  <action>
CHECKPOINT: Human verification required.

What was built: Complete replay pipeline -- landing page with Match ID entry, backend pipeline with BullMQ worker, SSE status updates, and PostgreSQL data storage.

How to verify:
1. Start services: `npm run docker:up` (starts PostgreSQL, Redis, odota/parser)
2. Push database schema: `npm run db:push`
3. Start the app and worker: `npm run dev:all`
4. Open http://localhost:3000 in browser
5. Verify hero section with DotaGenius branding, search bar, and Recent Analyses sections
6. Try submitting an invalid Match ID (e.g., "abc") -- should show validation error
7. Submit a valid recent Match ID -- verify redirect to /match/[matchId] status page
8. Watch StatusTimeline update through downloading -> parsing -> complete
9. Verify match summary data displays after completion
10. Go back to landing page -- verify match appears in "Your Recent Analyses"
11. Click recent analysis -- verify cached results with "Re-analyze" button
12. Try a very old Match ID -- should show expired replay warning
13. Try non-existent Match ID -- should show appropriate error
14. Open Prisma Studio (`npm run db:studio`) -- verify data in all 4 tables

Resume signal: Type "approved" if pipeline works end-to-end, or describe specific issues.
  </action>
  <verify>Human confirms end-to-end pipeline flow works correctly</verify>
  <done>Full pipeline verified: Match ID entry -> job submission -> replay download -> parsing -> PostgreSQL storage -> results display, with proper error handling for expired and invalid matches</done>
</task>
</task>

</tasks>

<verification>
- All unit tests pass: `npx vitest run tests/unit/`
- TypeScript compiles: `npx tsc --noEmit`
- Integration test files exist with proper structure
- Worker starts successfully with `npm run worker`
- Full pipeline flow works: Match ID -> job -> parse -> data in DB -> results displayed
</verification>

<success_criteria>
- User can enter a Match ID on the landing page and see the pipeline progress in real-time
- Parsed data (players, combat logs, position snapshots) is stored in PostgreSQL and viewable
- Already-analyzed matches return cached results immediately
- Error states display clear messages for expired/unavailable/corrupt replays
- Worker runs as a separate process and doesn't block the web server
- All unit tests pass, integration tests are structured and guarded for CI
</success_criteria>

<output>
After completion, create `.planning/phases/01-replay-pipeline/01-05-SUMMARY.md`
</output>
