---
phase: 02-match-overview
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/match/LaningBreakdown.tsx
  - src/components/match/LaneCard.tsx
  - src/components/match/GoldXpGraphs.tsx
  - src/app/match/[matchId]/page.tsx
autonomous: false
requirements: [MATC-02, MATC-03]

must_haves:
  truths:
    - "User sees three per-lane cards (Top, Mid, Bot) with hero matchup, CS@10, and lane outcome"
    - "User sees mini CS line chart in each lane card showing LH accumulation for first 10 minutes"
    - "User sees separate section for jungling and roaming players"
    - "User sees tabbed graph interface with Advantage and Per Player tabs"
    - "User sees advantage graph showing Radiant gold/XP lead over time as single line"
    - "User sees per-player graph with 10 lines color-coded by team"
    - "User sees hover tooltips on graphs showing exact values at game time"
    - "User sees clickable objective markers on the graph timeline"
  artifacts:
    - path: "src/components/match/LaningBreakdown.tsx"
      provides: "Three lane cards + roaming section"
      min_lines: 50
    - path: "src/components/match/LaneCard.tsx"
      provides: "Single lane matchup card with mini CS chart"
      min_lines: 60
    - path: "src/components/match/GoldXpGraphs.tsx"
      provides: "Tabbed advantage and per-player graphs with tooltips and objective markers"
      min_lines: 120
  key_links:
    - from: "src/components/match/LaningBreakdown.tsx"
      to: "src/lib/match-utils.ts"
      via: "getLaneMatchups, getRoamingPlayers"
      pattern: "getLaneMatchups|getRoamingPlayers"
    - from: "src/components/match/LaneCard.tsx"
      to: "src/lib/match-utils.ts"
      via: "getCsAt10 for CS@10 extraction"
      pattern: "getCsAt10"
    - from: "src/components/match/LaneCard.tsx"
      to: "recharts"
      via: "Mini LineChart for CS accumulation"
      pattern: "LineChart|ResponsiveContainer"
    - from: "src/components/match/GoldXpGraphs.tsx"
      to: "src/lib/match-utils.ts"
      via: "buildAdvantageData, buildPerPlayerData"
      pattern: "buildAdvantageData|buildPerPlayerData"
    - from: "src/components/match/GoldXpGraphs.tsx"
      to: "recharts"
      via: "LineChart, Tooltip, ReferenceLine for interactive graphs"
      pattern: "LineChart|Tooltip|ReferenceLine"
    - from: "src/app/match/[matchId]/page.tsx"
      to: "src/components/match/LaningBreakdown.tsx"
      via: "Composing laning into match page"
      pattern: "LaningBreakdown"
    - from: "src/app/match/[matchId]/page.tsx"
      to: "src/components/match/GoldXpGraphs.tsx"
      via: "Composing graphs into match page"
      pattern: "GoldXpGraphs"
---

<objective>
Build the laning breakdown (three lane cards with hero matchups, CS@10, mini CS charts, and roaming section) and gold/XP graphs (tabbed advantage and per-player views with Recharts, hover tooltips, and objective markers). Wire both into the match page and verify the complete match overview visually.

Purpose: Deliver MATC-02 (laning breakdown) and MATC-03 (gold/XP graphs), completing the Phase 2 match overview. Users can now see their lane performance and team economy trends.

Output: LaningBreakdown, LaneCard, GoldXpGraphs components wired into the match page. Human-verified complete match overview.
</objective>

<execution_context>
@/home/rahulrajput/.claude/get-shit-done/workflows/execute-plan.md
@/home/rahulrajput/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-match-overview/02-CONTEXT.md
@.planning/phases/02-match-overview/02-RESEARCH.md
@.planning/phases/02-match-overview/02-01-SUMMARY.md
@.planning/phases/02-match-overview/02-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 01 and 02 that this plan consumes -->

From src/lib/match-types.ts:
```typescript
export interface PlayerData { /* all fields -- see Plan 01 */ }
export interface MatchResponse { /* all fields -- see Plan 01 */ }
export interface AdvantagePoint { minute: number; gold: number; xp: number }
export interface PerPlayerPoint { minute: number; [playerKey: string]: number }
export interface ObjectiveEvent { type: string; time: number; team?: number; key?: string; player_slot?: number }
```

From src/lib/match-utils.ts:
```typescript
export function getLaneMatchups(players: PlayerData[]): Record<number, { radiant: PlayerData[]; dire: PlayerData[] }>;
export function getRoamingPlayers(players: PlayerData[]): PlayerData[];
export function getCsAt10(lhT: number[] | null): number;
export function getDnAt10(dnT: number[] | null): number;
export function buildAdvantageData(goldAdv: number[] | null, xpAdv: number[] | null): AdvantagePoint[];
export function buildPerPlayerData(players: PlayerData[]): PerPlayerPoint[];
export const LANE_LABELS: Record<number, string>;
```

From src/lib/dota-constants.ts:
```typescript
export function getHero(heroId: number): HeroInfo | null;
```

From src/hooks/useFocusPlayer.ts:
```typescript
export function useFocusPlayer(): { focusSlot: number | null; setFocus: (slot: number) => void };
```

Recharts v3 patterns (from RESEARCH.md):
- Use SVG render order for z-index: place Tooltip AFTER all Line elements in JSX
- Do NOT use Customized component with CategoricalChartState (removed in v3)
- Use ResponsiveContainer for responsive sizing
- ReferenceLine for zero-axis in advantage graph

CSS custom properties:
- --card-bg: #111118, --card-border: #1e1e2e
- --success: #22c55e, --warning: #eab308, --error: #ef4444, --muted: #71717a
- --accent: #3b82f6
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LaningBreakdown, LaneCard, and GoldXpGraphs components</name>
  <files>src/components/match/LaningBreakdown.tsx, src/components/match/LaneCard.tsx, src/components/match/GoldXpGraphs.tsx</files>
  <action>
    Create `src/components/match/LaneCard.tsx`:
    - Props: `{ lane: number, radiantPlayers: PlayerData[], direPlayers: PlayerData[], focusSlot: number | null }`
    - Card layout showing lane label from `LANE_LABELS[lane]` (Top, Mid, Bot).
    - Hero matchup display: Radiant hero icons on left, "vs" in middle, Dire hero icons on right. Use `getHero()` for icons.
    - CS@10 for each player using `getCsAt10(player.lhT)`. Display as "CS@10: {value}".
    - Lane outcome: Compare laneEfficiency between the matchup players. The team with higher combined laneEfficiency "won" the lane. Show as colored indicator (green = won, red = lost, yellow = draw if within 0.05).
    - Mini CS line chart: Use Recharts `<LineChart>` in a small `<ResponsiveContainer>` (width="100%", height={80}). Plot lhT values for first 10 entries (minutes 0-10) for each player in the lane. Radiant lines in green tones, Dire lines in red tones. No axes labels needed for the mini chart, just the lines. `dot={false}`, thin lines.
    - Handle cases where a lane has 0, 1, 2, or 3+ players per team (trilanes, dual mids). Show all players present.
    - If no parsed data (lhT is null), show "No laning data" instead of chart.
    - Apply highlight/border if any player in the lane matches `focusSlot`.
    - Use card styling: `rounded-xl bg-[var(--card-bg)] border border-[var(--card-border)] p-4`.

    Create `src/components/match/LaningBreakdown.tsx`:
    - Props: `{ players: PlayerData[], focusSlot: number | null }`
    - Use `getLaneMatchups(players)` to get lane groupings.
    - Render three `<LaneCard>` components in a responsive grid (3 cols on lg, 1 col on mobile).
    - Below the lane cards, render a "Roaming / Jungle" section using `getRoamingPlayers(players)`.
    - For roaming players: show hero icon, player name, and any relevant stats (camps stacked, rotations if available).
    - If no roaming players, hide the section entirely.
    - Section title: "Laning Phase" with a subtitle "First 10 minutes".

    Create `src/components/match/GoldXpGraphs.tsx`:
    - Props: `{ match: MatchResponse }`
    - Tabbed interface with two tabs: "Advantage" (default active) and "Per Player".
    - Tab buttons styled with card-border background, active tab with accent underline.

    **Advantage tab:**
    - Use `buildAdvantageData(match.radiantGoldAdv, match.radiantXpAdv)` to get chart data.
    - Recharts `<LineChart>` inside `<ResponsiveContainer width="100%" height={300}>`.
    - `<CartesianGrid strokeDasharray="3 3" stroke="var(--card-border)" />`
    - `<XAxis dataKey="minute" stroke="var(--muted)" />` with label "Minutes"
    - `<YAxis stroke="var(--muted)" />` with tick formatter showing +/- values
    - `<ReferenceLine y={0} stroke="var(--muted)" strokeDasharray="3 3" />` for zero line
    - `<Line type="monotone" dataKey="gold" stroke="var(--warning)" dot={false} strokeWidth={2} name="Gold Advantage" />`
    - `<Line type="monotone" dataKey="xp" stroke="var(--accent)" dot={false} strokeWidth={2} name="XP Advantage" />`
    - `<Tooltip>` placed AFTER Line elements (v3 render order). Custom tooltip component showing "Minute X: Gold +/-Y, XP +/-Z". Dark background tooltip matching theme.
    - Area above zero tinted slightly green (Radiant ahead), below zero tinted slightly red (Dire ahead) -- use `<Area>` or CSS, Claude's discretion.

    **Per Player tab:**
    - Use `buildPerPlayerData(match.players)` to get chart data.
    - 10 individual `<Line>` components, one per player.
    - Color palette: 5 shades of green/teal for Radiant players, 5 shades of red/orange for Dire players. Claude's discretion on exact hex values that contrast well against #111118 dark background.
    - `<Tooltip>` with custom component showing all 10 player values at the hovered minute, sorted by gold. Show hero names (via getHero) not just "player_0".
    - Legend showing hero icons or names with their line colors.

    **Objective markers** (both tabs):
    - If `match.objectives` exists, render clickable markers on the graph timeline for: tower kills, Roshan kills, first blood, courier kills, barracks destructions.
    - Use Recharts `<ReferenceLine x={Math.floor(event.time / 60)}` with a custom label or dot.
    - Marker icons/colors: towers = gray, Roshan = yellow, first blood = red. Keep it simple -- small dots or vertical dashed lines with hover labels.
    - If no objectives data, skip markers entirely.

    **Empty data handling:**
    - If `buildAdvantageData` returns empty array, show "No parsed data available for graphs" banner instead of empty chart.
    - If a player's goldT is null in per-player view, omit that player's line.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/unit/match-utils.test.ts --reporter=verbose</automated>
  </verify>
  <done>LaningBreakdown shows 3 lane cards with hero matchups, CS@10, lane outcomes, and mini CS charts. GoldXpGraphs shows tabbed advantage/per-player views with interactive tooltips and objective markers. All components compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire laning and graphs into match page</name>
  <files>src/app/match/[matchId]/page.tsx</files>
  <action>
    Update `src/app/match/[matchId]/page.tsx`:

    1. Import LaningBreakdown and GoldXpGraphs components.
    2. In the "complete" match view (where MatchHeader and Scoreboard are already rendered from Plan 02):
       - Replace the `{/* Laning Breakdown - Plan 03 */}` placeholder with `<LaningBreakdown players={matchData.players} focusSlot={focusSlot} />`
       - Replace the `{/* Gold/XP Graphs - Plan 03 */}` placeholder with `<GoldXpGraphs match={matchData} />`
    3. Layout order: MatchHeader -> Scoreboard -> Laning Breakdown -> Gold/XP Graphs -> Re-analyze button.
    4. Add spacing between sections (e.g., `space-y-8` on the container or `mt-8` on each section).
    5. Verify the complete page renders without errors with `npm run build`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Match page composes all Phase 2 components: header, scoreboard, laning breakdown, and gold/XP graphs. Next.js build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete match overview page</name>
  <files>src/app/match/[matchId]/page.tsx</files>
  <action>
    Human verifies the complete match overview page visually. All automated work is complete -- this checkpoint confirms the UI renders correctly with real match data.

    What was built: Complete match overview page with header (5v5 hero portraits, scores, metadata), scoreboard (two team tables with all stats, items, benchmarks, expandable details), laning breakdown (three lane cards with CS charts), and gold/XP graphs (tabbed advantage + per-player with tooltips).

    How to verify:
    1. Start the dev server: `npm run dev` (ensure Docker services are running: `docker compose up -d`)
    2. Navigate to a completed match page (e.g., http://localhost:3000/match/{matchId} where matchId is a previously analyzed match)
    3. Verify the Match Header:
       - 5 Radiant hero icons on left, score in middle, 5 Dire hero icons on right
       - Winning team has a visible glow/highlight
       - Metadata row shows match ID, region, date, lobby type, duration, game mode
    4. Verify the Scoreboard:
       - Two tables (Radiant, then Dire) with all stat columns
       - Item builds show 6 items + neutral item with icons
       - Some stat values have color coding (green/yellow/red from benchmarks)
       - Click a player row -- it highlights as focus player
       - Click the expand icon on a row -- detail panel opens with purchase log, abilities, damage, wards
    5. Verify the Laning Breakdown:
       - Three lane cards (Top, Mid, Bot) showing hero matchups
       - CS@10 values displayed for each laner
       - Mini CS line charts visible in each card
       - Lane outcome indicator (won/lost/draw)
    6. Verify Gold/XP Graphs:
       - "Advantage" tab shows single gold + XP lines with zero reference line
       - Hover shows tooltip with exact values
       - Switch to "Per Player" tab -- 10 colored lines appear
       - Objective markers visible on timeline (if match has objective data)
    7. If no previously analyzed match exists, submit one first via the home page
  </action>
  <verify>Visual inspection by user</verify>
  <done>User confirms all Phase 2 components render correctly with real match data. Type "approved" or describe issues to fix.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- zero TypeScript errors
- `npm run build` -- Next.js build succeeds
- `npx vitest run` -- all tests pass (no regressions)
- Visual verification of complete match overview page
</verification>

<success_criteria>
- Three lane cards show correct hero matchups per lane with CS@10 and mini CS charts
- Roaming/jungle section appears when applicable
- Advantage graph shows gold and XP lead lines with zero reference
- Per-player graph shows 10 colored lines (5 green-toned, 5 red-toned)
- Hover tooltips display exact values on both graph types
- Objective markers appear on graph timeline
- Empty/null data handled gracefully with fallback messages
- Complete match overview page passes human visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-match-overview/02-03-SUMMARY.md`
</output>
